<!DOCTYPE html>
<html lang="en-IN">
    <head>
        <meta charset="utf-8"/>
        <title>App</title>

        <link href='{{ base_url() }}/css/style.css' rel='stylesheet' type='text/css'>
    </head>
    <body>
        <h1>Hello App</h1>
        <ul>
            {% for book in books %}
                <li>{{ book.name }}</li>
                {% else %}
                <small>No users have been found.</small>
            {% endfor %}
        </ul>
    </body>
</html>
